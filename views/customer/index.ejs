      <!DOCTYPE html>
      <html lang="en">
      <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Sher Ali's Store</title>
        <meta name="description" content="Welcome to Sher Ali's online store - Quality products at great prices">
        <link rel="stylesheet" href="/css/style.css">
        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <style>
          :root {
            --primary: #4CAF50;
            --primary-dark: #3e8e41;
            --secondary: #EF4444;
            --dark: #333;
            --light: #f8f9fa;
            --gray: #6c757d;
            --border: #e9ecef;
            --shadow: 0 5px 15px rgba(0,0,0,0.08);
            --logo-color: #2c3e50;
            --logo-highlight: #e74c3c;
          }

          body {
            font-family: 'Poppins', sans-serif;
            background-color: #f5f5f5;
            color: var(--dark);
            line-height: 1.6;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
          }

          /* Header Styles */
          header {
            background-color: white;
            box-shadow: var(--shadow);
            padding: 15px 0;
            position: sticky;
            top: 0;
            z-index: 100;
          }

          .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
          }

          /* Logo Styles */
          .logo {
            display: flex;
            align-items: center;
            text-decoration: none;
          }

          .logo-icon {
            font-size: 28px;
            color: var(--logo-highlight);
            margin-right: 10px;
          }

          .logo-text {
            font-size: 24px;
            font-weight: 700;
            background: linear-gradient(to right, var(--logo-color) 70%, var(--logo-highlight) 30%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-fill-color: transparent;
          }

          .logo-text span {
            font-weight: 300;
          }

          /* Top Navigation */
          .top-nav {
            display: flex;
            align-items: center;
            gap: 15px;
          }

          .top-nav a {
            text-decoration: none;
            padding: 8px 16px;
            background: #007BFF;
            color: white;
            border-radius: 5px;
            font-size: 14px;
            transition: all 0.3s;
          }

          .top-nav a:hover {
            background: #0056b3;
            transform: translateY(-2px);
          }

          /* Main Container */
          .container {
            max-width: 1200px;
            margin: 30px auto;
            padding: 0 20px;
            flex: 1;
          }

          /* Search Bar */
          .search-container {
            margin: 20px 0;
            position: relative;
          }

          .search-input {
            width: 100%;
            padding: 12px 20px;
            border: 1px solid var(--border);
            border-radius: 30px;
            font-size: 16px;
            box-shadow: var(--shadow);
            transition: all 0.3s;
          }

          .search-input:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(76, 175, 80, 0.2);
          }

          .search-btn {
            position: absolute;
            right: 5px;
            top: 5px;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 50px;
            padding: 7px 15px;
            cursor: pointer;
            transition: all 0.3s;
          }

          .search-btn:hover {
            background: var(--primary-dark);
          }

          /* Product Grid */
          .product-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr); /* 4 columns on desktop */
            gap: 20px;
            margin-top: 20px;
          }

          .product-card {
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: all 0.3s;
            display: flex;
            flex-direction: column;
          }

          .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
          }

          .product-image-container {
            height: 200px;
            position: relative;
            overflow: hidden;
            background: #f8f9fa;
            flex-shrink: 0;
          }

          .product-image {
            width: 100%;
            height: 100%;
            object-fit: contain;
            transition: transform 0.5s;
          }

          .product-card:hover .product-image {
            transform: scale(1.05);
          }

          .product-details {
            padding: 15px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
          }

          .product-variants {
            color: var(--gray);
            font-size: 13px;
            margin-bottom: 8px;
          }

          .product-title {
            font-size: 15px;
            font-weight: 500;
            margin-bottom: 8px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
          }

          .product-rating {
            display: flex;
            align-items: center;
            margin-bottom: 8px;
          }

          .stars {
            color: #ffc107;
            font-size: 14px;
            margin-right: 5px;
          }

          .rating-count {
            color: var(--gray);
            font-size: 13px;
          }

          .purchase-info {
            font-size: 13px;
            color: var(--gray);
            margin-bottom: 12px;
          }

          .product-price {
            font-size: 18px;
            font-weight: 600;
            color: var(--primary);
            margin-bottom: 15px;
          }

          .product-actions {
            display: flex;
            justify-content: space-between;
            margin-top: auto;
          }

          .btn {
            padding: 8px 15px;
            border-radius: 4px;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 5px;
            border: none;
          }

          .btn-primary {
            background: var(--primary);
            color: white;
          }

          .btn-secondary {
            background: white;
            color: var(--secondary);
            border: 1px solid var(--secondary) !important;
          }

          .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
          }

          /* Favorites Icon - Mobile Optimized */
          .favorite-icon {
            position: fixed;
            bottom: 20px;
            right: 20px;
            font-size: 22px;
            cursor: pointer;
            background: white;
            border-radius: 50%;
            padding: 15px;
            box-shadow: var(--shadow);
            z-index: 99;
            transition: all 0.3s;
            border: none;
            width: 60px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
          }

          .favorite-icon:hover {
            transform: scale(1.1);
          }

          .favorite-count {
            position: absolute;
            top: 5px;
            right: 5px;
            background: var(--secondary);
            color: white;
            border-radius: 50%;
            font-size: 12px;
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
          }

          .favorites-sidebar {
            position: fixed;
            top: 0;
            right: -100%;
            width: 90%;
            max-width: 360px;
            height: 100%;
            background: white;
            box-shadow: -2px 0 15px rgba(0,0,0,0.1);
            transition: right 0.3s ease;
            z-index: 1000;
            padding: 20px;
            overflow-y: auto;
          }

          .favorites-sidebar.open {
            right: 0;
          }

          .favorites-sidebar h3 {
            margin: 10px 0 20px;
            font-size: 22px;
            color: var(--dark);
            display: flex;
            align-items: center;
            gap: 10px;
          }

          .favorites-sidebar h3 i {
            color: var(--secondary);
          }

          .close-btn {
            position: absolute;
            top: 15px;
            right: 20px;
            font-size: 24px;
            cursor: pointer;
            color: var(--gray);
            background: none;
            border: none;
            transition: all 0.3s;
          }

          .close-btn:hover {
            color: var(--secondary);
            transform: rotate(90deg);
          }

          .favorite-item {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
            border-bottom: 1px solid var(--border);
            padding-bottom: 15px;
          }

          .favorite-item img {
            width: 70px;
            height: 70px;
            object-fit: contain;
            margin-right: 15px;
            background: #f8f9fa;
            border-radius: 6px;
          }

          .favorite-info {
            flex-grow: 1;
          }

          .favorite-info h4 {
            margin: 0 0 5px;
            font-size: 16px;
          }

          .favorite-info p {
            color: var(--primary);
            font-weight: 600;
            margin-bottom: 8px;
          }

          .favorite-qty {
            display: flex;
            align-items: center;
            margin-bottom: 8px;
          }

          .favorite-qty input {
            width: 50px;
            padding: 5px;
            border: 1px solid var(--border);
            border-radius: 4px;
            margin-right: 10px;
          }

          .btn-small {
            padding: 6px 12px;
            font-size: 13px;
            border-radius: 4px;
            border: none;
            cursor: pointer;
            transition: all 0.3s;
          }

          .btn-add {
            background: var(--primary);
            color: white;
            margin-right: 5px;
          }

          .btn-remove {
            background: var(--secondary);
            color: white;
          }

          .btn-small:hover {
            transform: translateY(-2px);
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
          }

          /* Empty State */
          .empty-state {
            text-align: center;
            padding: 40px 0;
          }

          .empty-state i {
            font-size: 50px;
            color: var(--gray);
            margin-bottom: 20px;
          }

          .empty-state p {
            color: var(--gray);
            font-size: 16px;
          }

          /* Footer Styles */
          footer {
            background-color: var(--dark);
            color: white;
            padding: 40px 0 20px;
            margin-top: 40px;
          }

          .footer-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 30px;
          }

          .footer-column h4 {
            font-size: 18px;
            margin-bottom: 20px;
            position: relative;
            padding-bottom: 10px;
          }

          .footer-column h4::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: 0;
            width: 40px;
            height: 2px;
            background: var(--primary);
          }

          .footer-links {
            list-style: none;
            padding: 0;
            margin: 0;
          }

          .footer-links li {
            margin-bottom: 10px;
          }

          .footer-links a {
            color: #ddd;
            text-decoration: none;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 8px;
          }

          .footer-links a:hover {
            color: white;
            transform: translateX(5px);
          }

          .footer-links a i {
            font-size: 14px;
          }

          .footer-contact p {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
          }

          .social-links {
            display: flex;
            gap: 15px;
            margin-top: 20px;
          }

          .social-links a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: rgba(255,255,255,0.1);
            color: white;
            transition: all 0.3s;
          }

          .social-links a:hover {
            background: var(--primary);
            transform: translateY(-3px);
          }

          .footer-bottom {
            text-align: center;
            padding-top: 20px;
            margin-top: 30px;
            border-top: 1px solid rgba(255,255,255,0.1);
            font-size: 14px;
            color: #aaa;
          }

          /* Responsive Design */
          @media (min-width: 768px) {
            /* Desktop styles for favorite icon */
            .favorite-icon {
              top: 50%;
              right: 0;
              transform: translateY(-50%);
              border-radius: 50% 0 0 50%;
              padding: 15px 12px 15px 18px;
              bottom: auto;
            }

            .favorite-icon:hover {
              right: 5px;
            }
          }

          @media (max-width: 768px) {
            .product-grid {
              grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            }

            .header-container {
              flex-direction: column;
              gap: 15px;
            }

            .top-nav {
              width: 100%;
              justify-content: center;
            }
          }

          @media (max-width: 480px) {
            .product-grid {
              grid-template-columns: 1fr;
            }

            .product-card {
              max-width: 100%;
            }

            .top-nav a {
              padding: 8px 12px;
              font-size: 13px;
            }

            .logo-text {
              font-size: 20px;
            }

            .logo-icon {
              font-size: 24px;
            }

            .search-input {
              padding: 10px 15px;
            }

            .search-btn {
              padding: 5px 10px;
            }
          }
        </style>
      </head>
      <body>
        <!-- Header with Logo -->
        <header>
          <div class="header-container">
            <a href="/" class="logo">
              <i class="fas fa-store logo-icon"></i>
              <div class="logo-text">SHER<span>ALI</span></div>
            </a>
            <nav class="top-nav">
              <a href="/auth/customer/login"><i class="fas fa-sign-in-alt"></i> Login</a>
              <a href="/auth/customer/register"><i class="fas fa-user-plus"></i> Register</a>
            </nav>
          </div>
        </header>

        <!-- 🛍️ Favorite Icon - Optimized Position -->
        <button class="favorite-icon" onclick="toggleFavorites()" aria-label="View favorites">
          <i class="fas fa-heart"></i>
          <div class="favorite-count" id="favorite-count">0</div>
        </button>

        <!-- 🧾 Sidebar Favorites -->
        <aside class="favorites-sidebar" id="favorites-sidebar">
          <button class="close-btn" onclick="toggleFavorites()" aria-label="Close favorites">&times;</button>
          <h3><i class="fas fa-heart"></i> Your Favorites</h3>
          <div id="favorites-list"></div>
        </aside>

        <!-- Main Content -->
        <main class="container">
          <!-- Search Bar -->
          <div class="search-container">
            <input type="text" class="search-input" id="search-input" placeholder="Search for products..." aria-label="Search products">
            <button class="search-btn" id="search-btn" aria-label="Search"><i class="fas fa-search"></i></button>
          </div>

          <!-- Product Grid -->
          <% if (products.length === 0) { %>
            <div class="empty-state">
              <i class="fas fa-box-open" aria-hidden="true"></i>
              <p>No products available yet.</p>
            </div>
          <% } else { %>
            <div class="product-grid" id="product-grid">
              <% products.forEach(product => { %>
                <article class="product-card">
                  <a href="/product/<%= product.id %>">
                    <div class="product-image-container">
                      <img src="<%= product.images?.[0] || '/images/default.jpg' %>" 
                           alt="<%= product.name %>" 
                           class="product-image"
                           loading="lazy">
                    </div>
                  </a>
                  <div class="product-details">

                    <h3 class="product-title"><%= product.name %></h3>
                    <div class="product-rating">
                      <span class="stars">
                        <% const rating = product.rating || 4.5; %>
                        <% for (let i = 1; i <= 5; i++) { %>
                          <% if (i <= Math.floor(rating)) { %>
                            <i class="fas fa-star"></i>
                          <% } else if (i === Math.ceil(rating) && rating % 1 > 0) { %>
                            <i class="fas fa-star-half-alt"></i>
                          <% } else { %>
                            <i class="far fa-star"></i>
                          <% } %>
                        <% } %>
                      </span>
                      <span class="rating-count">(<%= (product.ratingCount || 1000).toLocaleString() %>)</span>
                    </div>
                    <div class="purchase-info">
                      <% if (product.monthlySales) { %>
                        <%= product.monthlySales.toLocaleString() %>+ bought in past month
                      <% } %>
                      <% if (product.specialOffer) { %>
                        <span style="color: var(--secondary);"><%= product.specialOffer %></span>
                      <% } %>
                    </div>
                    <div class="product-price">$<%= product.price.toFixed(2) %></div>
                    <div class="product-actions">
                      <button class="btn btn-primary" onclick="addToCart('<%= product.id %>')">
                        <i class="fas fa-shopping-cart"></i> Add to Cart
                      </button>
                      <button class="btn btn-secondary" onclick="addToFavorites(<%- JSON.stringify(product) %>)" aria-label="Add to favorites">
                        <i class="fas fa-heart"></i>
                      </button>
                    </div>
                  </div>
                </article>
              <% }) %>
            </div>
          <% } %>
        </main>

        <!-- Footer Section -->
        <footer>
          <div class="footer-container">
            <div class="footer-column">
              <h4>About Us</h4>
              <p>Sher Ali's Store is your one-stop shop for quality products at affordable prices. We're committed to providing excellent customer service and fast delivery.</p>
              <div class="social-links">
                <a href="#" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
                <a href="#" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
                <a href="#" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
                <a href="#" aria-label="YouTube"><i class="fab fa-youtube"></i></a>
              </div>
            </div>

            <div class="footer-column">
              <h4>Quick Links</h4>
              <ul class="footer-links">
                <li><a href="/"><i class="fas fa-chevron-right"></i> Home</a></li>
                <li><a href="/products"><i class="fas fa-chevron-right"></i> Products</a></li>
                <li><a href="/about"><i class="fas fa-chevron-right"></i> About Us</a></li>
                <li><a href="/contact"><i class="fas fa-chevron-right"></i> Contact</a></li>
                <li><a href="/faq"><i class="fas fa-chevron-right"></i> FAQ</a></li>
              </ul>
            </div>

            <div class="footer-column">
              <h4>Customer Service</h4>
              <ul class="footer-links">
                <li><a href="/account"><i class="fas fa-chevron-right"></i> My Account</a></li>
                <li><a href="/orders"><i class="fas fa-chevron-right"></i> Order Tracking</a></li>
                <li><a href="/returns"><i class="fas fa-chevron-right"></i> Returns Policy</a></li>
                <li><a href="/privacy"><i class="fas fa-chevron-right"></i> Privacy Policy</a></li>
                <li><a href="/terms"><i class="fas fa-chevron-right"></i> Terms & Conditions</a></li>
              </ul>
            </div>

            <div class="footer-column footer-contact">
              <h4>Contact Us</h4>
              <p><i class="fas fa-map-marker-alt"></i> 123 Store Street, City, Country</p>
              <p><i class="fas fa-phone-alt"></i> +1 (123) 456-7890</p>
              <p><i class="fas fa-envelope"></i> info@sheralistore.com</p>
              <p><i class="fas fa-clock"></i> Mon-Fri: 9AM - 6PM</p>
            </div>
          </div>

          <div class="footer-bottom">
            <p>&copy; <script>document.write(new Date().getFullYear())</script> Sher Ali's Store. All Rights Reserved.</p>
          </div>
        </footer>

        <!-- JavaScript -->
        <script>
          // Search functionality
          document.getElementById('search-btn').addEventListener('click', searchProducts);
          document.getElementById('search-input').addEventListener('keyup', function(e) {
            if (e.key === 'Enter') {
              searchProducts();
            }
          });

          function searchProducts() {
            const searchTerm = document.getElementById('search-input').value.toLowerCase().trim();
            const productCards = document.querySelectorAll('.product-card');

            productCards.forEach(card => {
              const title = card.querySelector('.product-title').textContent.toLowerCase();
              card.style.display = title.includes(searchTerm) ? 'block' : 'none';
            });
          }

          // Favorites functionality
          function getFavorites() {
            return JSON.parse(localStorage.getItem("favorites") || "[]");
          }

          function saveFavorites(favorites) {
            localStorage.setItem("favorites", JSON.stringify(favorites));
          }

          function updateFavoriteCount() {
            const favorites = getFavorites();
            const el = document.getElementById("favorite-count");
            if (el) el.textContent = favorites.length;
          }

          function toggleFavorites() {
            const sidebar = document.getElementById("favorites-sidebar");
            sidebar?.classList.toggle("open");
            if (sidebar?.classList.contains("open")) {
              renderFavorites();
            }
          }

          function showNotification(message, isError = false) {
            const notification = document.createElement('div');
            notification.style.position = 'fixed';
            notification.style.bottom = '20px';
            notification.style.right = '20px';
            notification.style.backgroundColor = isError ? 'var(--secondary)' : 'var(--primary)';
            notification.style.color = 'white';
            notification.style.padding = '15px 25px';
            notification.style.borderRadius = '6px';
            notification.style.boxShadow = '0 4px 12px rgba(0,0,0,0.15)';
            notification.style.zIndex = '1000';
            notification.style.animation = 'fadeIn 0.3s';
            notification.innerHTML = `<i class="fas ${isError ? 'fa-info-circle' : 'fa-check-circle'}"></i> ${message}`;
            document.body.appendChild(notification);

            setTimeout(() => {
              notification.style.animation = 'fadeOut 0.3s';
              setTimeout(() => notification.remove(), 300);
            }, 2000);
          }

          function addToFavorites(product) {
            const favorites = getFavorites();
            if (!favorites.some(p => p.id === product.id)) {
              product.quantity = 1;
              favorites.push(product);
              saveFavorites(favorites);
              updateFavoriteCount();
              showNotification('Added to favorites!');
            } else {
              showNotification('Already in favorites', true);
            }
          }

          function removeFromFavorites(productId) {
            let favorites = getFavorites();
            favorites = favorites.filter(p => p.id !== productId);
            saveFavorites(favorites);
            updateFavoriteCount();
            renderFavorites();
          }

          function updateQuantity(id, qty) {
            const favorites = getFavorites();
            const product = favorites.find(f => f.id === id);
            if (product) {
              product.quantity = Math.max(1, parseInt(qty) || 1);
              saveFavorites(favorites);
            }
          }

          function renderFavorites() {
            const list = document.getElementById("favorites-list");
            const favorites = getFavorites();

            if (!favorites.length) {
              list.innerHTML = `
                <div class="empty-state">
                  <i class="fas fa-heart" aria-hidden="true"></i>
                  <p>Your favorites list is empty</p>
                </div>
              `;
              return;
            }

            list.innerHTML = favorites.map(product => `
              <div class="favorite-item">
                <img src="${product.images?.[0] || '/images/default.jpg'}" alt="${product.name}" loading="lazy">
                <div class="favorite-info">
                  <h4>${product.name}</h4>
                  <p>$${product.price.toFixed(2)}</p>
                  <div class="favorite-qty">
                    <label>Qty: </label>
                    <input type="number" value="${product.quantity || 1}" min="1">
                  </div>
                  <form method="POST" action="/add-to-cart/${product.id}" style="display: inline;">
                    <input type="hidden" name="qty" value="${product.quantity || 1}">
                    <button class="btn-small btn-add" type="submit">
                      <i class="fas fa-cart-plus"></i> Add to Cart
                    </button>
                  </form>
                  <button class="btn-small btn-remove" onclick="removeFromFavorites('${product.id}')">
                    <i class="fas fa-trash"></i> Remove
                  </button>
                </div>
              </div>
            `).join('');

            // Add event listeners for quantity changes
            document.querySelectorAll('.favorite-item input[type="number"]').forEach(input => {
              input.addEventListener('change', (e) => {
                const productId = e.target.closest('.favorite-item').querySelector('.btn-remove').onclick
                  .toString().match(/'([^']+)'/)[1];
                updateQuantity(productId, e.target.value);
              });
            });
          }

          // Initialize
          document.addEventListener('DOMContentLoaded', function() {
            updateFavoriteCount();

            // Add CSS for animations
            const style = document.createElement('style');
            style.textContent = `
              @keyframes fadeIn {
                from { opacity: 0; transform: translateY(20px); }
                to { opacity: 1; transform: translateY(0); }
              }
              @keyframes fadeOut {
                from { opacity: 1; transform: translateY(0); }
                to { opacity: 0; transform: translateY(20px); }
              }
            `;
            document.head.appendChild(style);
          });
        </script>
      </body>
      </html>
